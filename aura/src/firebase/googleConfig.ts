/**
 * Google Sign-In configuration.
 * Key is read from src/env.ts (gitignored).
 * See src/env.example.ts for where to find the value.
 */
import { GOOGLE_WEB_CLIENT_ID } from '../env';

export { GOOGLE_WEB_CLIENT_ID };
